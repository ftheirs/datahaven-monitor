name: Stress – Manual

on:
  workflow_dispatch:
    inputs:
      testName:
        description: "Which stress test to run (fileUploadStress, downloadStorm, merkleBigFile, multiUserFlow)"
        required: true
        default: "fileUploadStress"
      concurrency:
        description: "Approximate concurrency level"
        required: false
        default: "10"
      duration:
        description: "Duration in seconds"
        required: false
        default: "300"

jobs:
  stress:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install

      - name: Build (placeholder)
        run: bun run build

      - name: Run stress test (placeholder)
        run: |
          echo "TODO: wire this workflow to src/stress/index.ts"
          echo "Requested test: ${{ github.event.inputs.testName }}"
          echo "Concurrency: ${{ github.event.inputs.concurrency }}"
          echo "Duration: ${{ github.event.inputs.duration }}"


